(()=>{
 const root=document.querySelector('[data-ai-quote]');if(!root)return;
 const model=root.querySelector('[data-ai-model]'),questions=root.querySelector('[data-ai-questions]'),days=root.querySelector('[data-ai-days]'),inputTokens=root.querySelector('[data-ai-input-tokens]'),outputTokens=root.querySelector('[data-ai-output-tokens]'),exchange=root.querySelector('[data-ai-exchange]'),margin=root.querySelector('[data-ai-margin]');
 const number=(field,fallback=0)=>Math.max(0,Number(field.value)||fallback),brl=value=>value.toLocaleString('pt-BR',{style:'currency',currency:'BRL',minimumFractionDigits:value<.01?4:2,maximumFractionDigits:value<.01?4:2}),usd=value=>`US$ ${value.toLocaleString('pt-BR',{minimumFractionDigits:value<1?4:2,maximumFractionDigits:value<1?4:2})}`,compact=value=>value.toLocaleString('pt-BR',{notation:value>=1000000?'compact':'standard',maximumFractionDigits:1});
 const presets={light:{questions:3,days:22,input:800,output:200},moderate:{questions:5,days:22,input:1200,output:300},intense:{questions:10,days:26,input:2000,output:500}};
 const calculate=()=>{const option=model.selectedOptions[0],inputPrice=Number(option.dataset.inputPrice),outputPrice=Number(option.dataset.outputPrice),queries=number(questions)*number(days),inputPerUser=queries*number(inputTokens),outputPerUser=queries*number(outputTokens),base=(inputPerUser/1000000*inputPrice)+(outputPerUser/1000000*outputPrice),costUsd=base*(1+number(margin)/100),rate=number(exchange,1),costBrl=costUsd*rate;
  root.querySelector('[data-ai-model-profile]').textContent=option.dataset.profile;document.querySelector('[data-ai-selected-model]').textContent=option.textContent;root.querySelector('[data-ai-requests-user]').textContent=compact(queries);root.querySelector('[data-ai-total-tokens-user]').textContent=compact(inputPerUser+outputPerUser);root.querySelector('[data-ai-cost-user-brl]').textContent=brl(costBrl);root.querySelector('[data-ai-cost-user-usd]').textContent=usd(costUsd);root.querySelector('[data-ai-cost-request]').textContent=brl(queries?costBrl/queries:0);
  document.querySelectorAll('[data-ai-scenario]').forEach(row=>{const users=Number(row.dataset.users),requests=queries*users,input=inputPerUser*users,output=outputPerUser*users,totalUsd=costUsd*users,totalBrl=totalUsd*rate;row.querySelector('[data-ai-requests]').textContent=compact(requests);row.querySelector('[data-ai-input]').textContent=compact(input);row.querySelector('[data-ai-output]').textContent=compact(output);row.querySelector('[data-ai-usd]').textContent=usd(totalUsd);row.querySelector('[data-ai-brl]').textContent=brl(totalBrl)})};
 [model,questions,days,inputTokens,outputTokens,exchange,margin].forEach(field=>field.addEventListener('input',calculate));document.querySelectorAll('[data-ai-preset]').forEach(button=>button.addEventListener('click',()=>{const preset=presets[button.dataset.aiPreset];questions.value=preset.questions;days.value=preset.days;inputTokens.value=preset.input;outputTokens.value=preset.output;document.querySelectorAll('[data-ai-preset]').forEach(item=>item.classList.toggle('active',item===button));calculate()}));calculate();
})();
